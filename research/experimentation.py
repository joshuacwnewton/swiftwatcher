"""
    Separate module containing versions of the swift counting algorithm
    with additional functionality for experimentation (e.g. loading
    frames from files, and visualizing frames at intermediate stages
    of the algorithm).
"""